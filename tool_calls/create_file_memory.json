{
  "name": "create_file_memory",
  "description": "This creates an embedding that is linked to a file in a database, for quick upload later, using cosine similarity.",
  "parameters": {
    "type": "object",
    "properties": {
      "Abstract": {
        "type": "string",
        "description": "All of the information required for the file memory should be put here in a way it can be embedded semantically."
      },
      "FilePath": {
        "type": "string",
        "description": "The filename of the file that the user is wanting to create a memory of."
      }
    },
    "required": [
      "Abstract",
      "FilePath"
    ]
  }
}