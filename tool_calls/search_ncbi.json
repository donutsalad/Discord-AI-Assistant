{
  "name": "search_ncbi",
  "description": "This function searches ncbi for articles relating to science.",
  "parameters": {
    "type": "object",
    "properties": {
      "Query": {
        "type": "string",
        "description": "The search query the user has provided."
      },
      "Count": {
        "type": "integer",
        "description": "This should be 1 if the user wants the top result, otherwise the number of results they'd like."
      }
    },
    "required": [
      "Query",
      "Count"
    ]
  }
}